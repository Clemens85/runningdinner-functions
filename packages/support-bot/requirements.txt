langgraph>=1.2.0
langchain-core>=0.3.0

pinecone

requests

pydantic>=2.0.0
pydantic-settings>=2.0.0

openai>=2.0.0
langsmith

# LLM Providers (add selectively as needed)
# Use the new unified Google Gen AI SDK (not the deprecated google-generativeai)
google-genai>=1.51.0

# Official LangGraph AWS checkpoint implementation (DynamoDB, no aioboto3 required)
langgraph-checkpoint-aws>=1.2.0


# AWS Lambda utilities (matching existing project patterns)
aws-lambda-powertools[tracer,logger,metrics]
